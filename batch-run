#!/bin/bash

IFS=$'\n'
for i in `seq -w 1 10`; do
	./run 0 2;
	sleep 1h && killall M3 && sleep 5s;
	if [ `ps aux | grep M3 | wc -l` -gt 1 ]; then
		echo Sorry, I couldn't kill IT...
		exit 1
	fi
	cp -a ./SavedFiles ./arch/tau2/$i
done
